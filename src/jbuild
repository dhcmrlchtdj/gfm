(jbuild_version 1)

(executable
  ((name gfm)
   ; (libraries ())
   ; (preprocess (pps ()))
   )
  )

; (library
 ; ((name ppx_let)
  ; (public_name ppx_let)
  ; (kind ppx_rewriter)
  ; (libraries (ppx_core ppx_driver))
  ; (preprocess no_preprocessing)))
